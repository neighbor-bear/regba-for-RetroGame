name: ReGBA for RetroFW Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive
        fetch-depth: 0

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential git bzip2 libsdl1.2-dev libpng-dev zlib1g-dev squashfs-tools

    - name: Setup RetroFW toolchain
      run: |
        sudo mkdir -p /opt
        # 使用更新的工具链链接
        wget https://github.com/retrofw/buildroot/releases/download/2021.01.14/retrofw-toolchain-mipsel-2021.01.14.tar.bz2
        sudo tar -xjf retrofw-toolchain-mipsel-2021.01.14.tar.bz2 -C /opt
        rm retrofw-toolchain-mipsel-2021.01.14.tar.bz2
        # 创建必要的符号链接
        sudo ln -s /opt/mipsel-RetroFW-linux-uclibc-2021.01.14 /opt/mipsel-RetroFW-linux-uclibc
        echo "/opt/mipsel-RetroFW-linux-uclibc/usr/bin" >> $GITHUB_PATH

    - name: Build ReGBA
      run: |
        make -j$(nproc)
        make opk
        make ipk

    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: regba-build
        path: |
          regba/regba.opk
          regba/regba.ipk
          regba/regba.dge
          regba/regba.elf
